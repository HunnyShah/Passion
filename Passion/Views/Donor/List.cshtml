@model IEnumerable<Passion.Models.DonorDto>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "New")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DonorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonorEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DonorBlood)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegistrationDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DonorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonorEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DonorBlood)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegistrationDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonorID }) |
            @Html.ActionLink("Delete", "DeleteConfirm", new { id=item.DonorID })
        </td>
    </tr>
}

</table>
